# SPEC-XXX: <title>

- **Intent:** INT-XXX
- **Status:** draft | committed | superseded
- **Skills applied:** api-standards, security-baseline, ui-brand (versions/ref)

## Data model
Entities, fields, relationships.

## API contract
Endpoints per `skills/api-standards`. Error shapes, pagination, validation.

## UI
Screens and flows per `skills/ui-brand`.

## Security requirements
Baked in per `skills/security-baseline` — validation, authz, rate limits, secrets handling.

## Acceptance criteria
Per feature, testable.
